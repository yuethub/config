#include <stdio.h>


Here's the "hyperlink" for the Vim tutor:
<vimref href="http://vimhelp.appspot.com/usr_01.txt.html#tutor">tutor</vimref>.

For more information on autocompletion see:
<vimref href="http://vimhelp.appspot.com/usr_01.txt.html#tutor">tutor</vimref>.

<vimref href="http://vimhelp.appspot.com/usr_01.txt.html#tutor">tutor</vimref>.

I love Paris in the 
the springtime.
double 
int

double

these their they

Neil, Andrew
Neil, Drew

class TangYueDocument < TangYueDocument; end
class TangYueTag < TangYueTag; end

This string contains a "quoted" word.
This string contains "two" quoted "words."
This "string doesn't make things easy."
#include <stdio.h>
I love Paris in the 
the springtime.


these their they

Neil, Andrew
Neil, Drew

class XHTMLDocument < XMLDocument; end
class XHTMLTag < XMLTag; end


var buttons = viewport.buttons;
viewport.buttons.previous.show();
viewport.buttons.next.show();
viewport.buttons.index.hide();

Match "quoted words"--- not quote marks;


patt
The N Key serches backward... key
...the \v pattern switch (a.k.a. very magic search)...

body    { color: #3c3c3c; }
a       { color: #0000EE; }
strong  { color: #000; }

double main(double argc, char *argv[])
{
    QApplication app(argc, char *argv);
    
    tangworld worldworld;
    hellonametang->show();
    return app.exec();
    fprintf()
}

void SelectionSort(int arr[], int n)
{
    for (int i = 0; i < n - 1; ++i)
        for (int j = i + 1; j < n; ++j)
        {
            if (a[i] < a[j]) 
            {
                int tmp = a[i];
                a[i] = a[j];
                a[j] = tmp;
            }
        }
}


helloname tang the tang is a hellonamehelloname worldl. worldl.
thank you for you hellonametang
tangworld worldworld worldworld worldworld



Match "quoted words"--- not quote marks;


patt
The N Key serches backward... key
...the \v pattern switch (a.k.a. very magic search)...

body    { color: #3c3c3c; }
a       { color: #0000EE; }
strong  { color: #000; }

double main(double argc, char *argv[])
{
    QApplication app(argc, char *argv);
    
    tangworld worldworld;
    hellonametang->show();
    return app.exec();
    fprintf()
}

void SelectionSort(int arr[], int n)
{
    for (int i = 0; i < n - 1; ++i)
        for (int j = i + 1; j < n; ++j)
        {
            if (a[i] < a[j]) 
            {
                int tmp = a[i];
                a[i] = a[j];
                a[j] = tmp;
            }
        }
}


helloname tang the tang is a hellonamehelloname worldl. worldl.
thank you for you hellonametang
tangworld worldworld worldworld worldworld
